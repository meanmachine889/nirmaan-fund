import Addresses from "@/components/addresses";
const Home = () => {
  return (
    <div className="flex items-center justify-center min-h-fit h-[100vh] w-[100%] font-[family-name:var(--font-geist-mono)]">
      <Addresses />
    </div>
  );
};

export default Home;
